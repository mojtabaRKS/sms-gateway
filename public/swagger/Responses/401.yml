description: Unauthorized
content:
  application/json:
    schema:
      properties:
        code:
          type: integer
          example: 401
        success:
          type: boolean
          example: false
        message:
          type: string
          example: "username or password is wrong"
        error:
          type: array
          items:
            type: string